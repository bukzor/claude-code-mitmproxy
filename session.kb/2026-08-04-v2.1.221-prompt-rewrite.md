# 2026-08-04: v2.1.221 rewrote the prompt for opus-5/fable-5; strip rate fell

## How it surfaced

Not by any alarm. In an unrelated session the user asked for an outline of
the served system prompt, then asked which parts looked bloated. Answering
that meant reading the prompt as delivered — which is post-patch, since
`ANTHROPIC_BASE_URL` pointed at the running proxy. Two things in that
outline should have been impossible: an unconditional Fast Mode line and a
full `gitStatus` block, both of which have patches.

## What changed upstream

v2.1.221 rewrote the prompt **for some models only**, and serves at least
two shapes concurrently:

| | shape | served to |
|---|---|---|
| new | `# Harness`, `# Scratchpad Directory`, `# Delivering work`, `# Corrections` | opus-5, fable-5 |
| old | `# System`, `# Doing tasks`, `# Executing actions with care`, `# Using your tools`, `# Tone and style`, `# Text output` | sonnet-5 |

Both keep `# Session-specific guidance`, `# Environment`,
`# Context management`. The 27k sonnet-5 captures are the old shape plus a
large session-optional `# auto memory` block (~130 lines) — that block, not
a third prompt shape, accounts for the size.

The long form served to opus-5 has converged on the `# Harness` shape that
`README.md` describes as the Fable-class variant. Whether the two are now
byte-identical in their shared sections is still unchecked.

Strip rate by shape:

| capture | original | patched | delta | rate |
|---|---|---|---|---|
| v2.1.221 opus-5 (new) | 8574 | 7824 | -750 | 8.7% |
| v2.1.221 fable-5 (new) | 9832 | 8705 | -1127 | 11.5% |
| v2.1.221 sonnet-5 (old) | 27874 | 23460 | -4414 | 15.8% |
| v2.1.214 long-form (old, promoted) | 17986 | 11103 | -6883 | 38.3% |
| v2.1.212-harness (promoted) | 8341 | 6779 | -1562 | 18.7% |

The regression is real and large — 38% → 9% on the shape opus-5 now gets —
but it is missing coverage of a *new* shape, not a failure against the old
one. The old shape is still served and still patched.

## Which patches fire where

`.` = match miss (silent, out of scope); `MATCH` = applies.

| patch | v2.1.214 | v221 opus-5 | v221 fable-5 | v221 sonnet-5 |
|---|---|---|---|---|
| `fix-tone-conciseness` | MATCH | . | . | MATCH |
| `strip-additional-dirs` | MATCH | . | . | . |
| `strip-backwards-compat` | MATCH | . | . | MATCH |
| `strip-colon-before-tools` | MATCH | . | . | MATCH |
| `strip-doing-tasks-bloat` | MATCH | . | . | MATCH |
| `strip-fast-mode-info` | . | . | . | . |
| `strip-git-status` | MATCH | . | . | . |
| `strip-help-feedback` | MATCH | . | . | MATCH |
| `strip-model-family` | MATCH | MATCH | MATCH | MATCH |
| `strip-output-efficiency` | . | . | . | . |
| `strip-security-bloat` | MATCH | MATCH | MATCH | MATCH |
| `strip-tool-preference` | . | . | . | . |
| `strip-url-restriction` | MATCH | . | . | MATCH |

Consequences:

- **A patch that matches the sonnet-5 column may not be sunset.** Six of
  them — `fix-tone-conciseness`, `strip-backwards-compat`,
  `strip-colon-before-tools`, `strip-doing-tasks-bloat`,
  `strip-help-feedback`, `strip-url-restriction` — target the old shape,
  which is still live. Marking those `upstream-removed` would convert a
  working patch into a false regression assertion.
- Genuine sunset candidates are only the ones that match nowhere:
  `strip-output-efficiency`, `strip-tool-preference`, and — pending its
  repair decision — `strip-fast-mode-info`.
- `strip-additional-dirs` matches the promoted v2.1.214 capture but no
  live shape; it needs its own check before being called either way.
- `strip-model-family` and `strip-security-bloat` are shape-independent
  and fire everywhere.

Two patches have a live target and miss it:

- `strip-git-status` — both `match.d/` variants are shape-wrong. v2.1.76
  expects no blank line after `gitStatus: $REST`; v2.1.128 expects a
  `Git user:` line. The live block has the blank line and no `Git user:`.
- `strip-fast-mode-info` — expects a `<fast_mode_info>` tag. The live text
  is an unconditional `# Environment` bullet. That patch's README already
  flagged itself UNVERIFIED on a stale-version smell; this confirms it,
  and confirms the cause is the wrong shape rather than a stale model name.

## Why nothing was loud

Working as designed, and that is the finding. A `match` miss is silent by
construction — the README argues, correctly, that a miss usually means the
patch is simply out of scope. When a whole cohort of patches goes out of
scope at once because upstream rewrote the document for one model class,
that same rule reports nothing. The `search`/`match` split only catches
drift *within* a scope that still exists.

`CLAUDE.md` already names this: "Promotion has no automatic trigger —
additive drift is invisible to every loud mechanism until someone looks."
This was that: not drift *added* to a known shape, but a new shape
appearing beside it. The standing-maintenance item that would have caught
it — promote captures newer than the newest full-body capture — was
outstanding, with v2.1.221 captures sitting unpromoted in
`log/prompt-captures/` and v2.1.214 the newest in `system-prompts.kb/`.

An aggregate strip-rate check is worth considering as a follow-on, since
no per-patch rule can see this class of failure. It must be **per-shape**:
a single global number is dominated by whichever shape happened to be
captured, and here the three live shapes strip at 9%, 11%, and 16%.

## Measurement prerequisite

Both patch directories are `~`-relative, so a shell with a wrong `$HOME`
resolves them somewhere that doesn't exist. Both loaders used to treat
that as "no patches" and return empty, which reports a clean zero-strip
run instead of failing. An empty patch set is never a legitimate
configuration for this proxy, so `load_patches` and `load_tool_patches`
now assert on both the missing directory and the empty result.

Note that an *unset* `$HOME` is not the hazard — `Path.expanduser()` falls
back to a `pwd` lookup by uid and resolves correctly. Only a wrong value
misresolves.

## Promotion (later same day) — and two corrections

Promoted the fullest capture per shape: `v2.1.221.md` (sonnet-5 long-form,
with `# auto memory`), `v2.1.221-opus.md`, `v2.1.221-fable.md` — naming
recorded in `system-prompts.kb/CLAUDE.md`. `check_patches` clean on all
three (−4414 / −750 / −1224); `check_dark_patches` warnings unchanged from
baseline (only the two known-explained v2.1.128 ones).

Corrections to the analysis above, from the fuller captures:

- **opus-5 and fable-5 get *different* new shapes**, not one. Both start
  `# Harness`, but fable has `# Communicating with the user` while opus has
  `# Delivering work`, `# Corrections`, and a session-optional `# Memory`.
  The table above conflates them.
- **`strip-additional-dirs` has a live target.** The matrix above ran
  against a fable capture whose session had no additional working
  directories; the fuller `b87` fable capture carries the block and the
  patch HITs it. Keep the patch; the sunset-vs-retarget question is closed.
- **`strip-output-efficiency` and `strip-tool-preference` were already
  sunset** (committed `upstream-removed.bool`, no `replace.md`/`search*`)
  before this session listed them as sunset candidates. Nothing to do.
  Note sonnet-5's live shape still has `# Using your tools` and `# Text
  output` *sections* — the matches miss on drifted content, not removed
  sections — so reviving either against the live sonnet text would be new
  patch work, not un-sunsetting.

## Repairs (same day)

- `strip-git-status`: the gitStatus block split *per model*, not just per
  version — every fable-5 capture carries `Git user:` (v2.1.128 shape),
  every opus-5 capture omits it. Added `match.d/v2.1.221.md` for the opus
  shape (dotfiles@4d06228). The "both variants miss" premise was half
  wrong: fable was being stripped all along — its matrix miss was a
  `check_dark_patches.py` false negative. The new shapes put gitStatus at
  end-of-body and captures have no trailing newline, but the tool skipped
  `apply_patches`' trailing-newline normalization; fixed (576dbd5).
- `strip-fast-mode-info`: retargeted (dotfiles@0c9b06c). Fast Mode is now
  an unconditional `# Environment` bullet, present in every captured body
  with that heading — so `match.md` anchors on the heading (loud if the
  bullet vanishes), `search.d/v2.1.221.md` matches the bullet with its
  churning version tail as `$REST`, and `search.d/v2.1.76.md` keeps the
  old conditional tag covered. UNVERIFIED caveat cleared.

Post-repair strip deltas: opus −1276 (was −750), fable −1394 (−1224),
sonnet −4584 (−4414); v2.1.214 −7055. Dark matrix truthful and warnings
still only the two known v2.1.128 ones.

Found in passing: `~/.claude/system-prompt-patches.d/strip-help-feedback`
has **uncommitted changes dated 2026-07-13** — `search.md`'s text moved
into `match.md`, `search.md` deleted. Behavior-neutral on every known
fixture (the text only ever appears under `# Doing tasks`), but it trades
the loud in-scope miss for a silent one, reversing the c910062 loudness
split for that patch. No session record explains it. Left in place,
flagged in todo. **Resolved later same day: reverted** -- no record, no
README, no functional gain, and it undid a deliberate committed design;
diff preserved at
`~/trash/2026-08-04-strip-help-feedback-uncommitted-jul13.diff`.
Dark-matrix warnings unchanged after revert.

## Side observations

- `tool-description-patches.d/` covers only `Monitor` and `SendMessage`,
  neither of which is served to opus-5 in the current shape. The largest
  built-in description served is `EndConversation` — the never-taken path,
  carrying a full policy plus a subsection on background forks calling it.
- The new prompt injects an agent-types listing alongside a standing
  `Do not call the AgentTool unless the user requested it` line. The two
  directly undercut each other.
- The parallel-tool-call directive appears twice in the new shape: in
  `# Harness` and again as the closing line.
- `# Delivering work` (~450 words) and `# Corrections` (~250 words) each
  restate a single instruction five to seven times — accreted patch-on-
  patch prose rather than an edited section. Together they are the largest
  reduction available in the new shape.

## New patches (same day) — and two more premise corrections

Tooling first: `survey_captures.py` (capture inventory: shape,
optional blocks, promoted-yet — the "someone looks" step) and
`check_dark_patches.py --pattern` (presence row: `-`/STRIPPED/SURVIVES
per fixture), both used heavily below.

Two of the three side observations above were wrong about *where* the
offending text lives:

- **The "duplicate" parallel-tools closing line is not in the prompt
  body.** No capture carries it twice; the closing line delivered to
  sessions is the API server's tool-use preamble, generated from the
  `tools` param — it never appears in the request, so no proxy patch can
  touch it. Confirmed by walking a day of traffic jsonl: the phrase
  occurs in `system[].text` only as sonnet's single in-body bullet. The
  duplication is real but *cross-surface*, so the patch strips the
  reachable half: `strip-duplicate-parallel-tools` (dotfiles@0485a57)
  removes the harness bullet's trailing sentence (opus/fable) and
  sonnet's whole ~380-char bullet (byte-stable v2.1.76→221).
- **The agent-types listing is not in the prompt body either** — it
  rides in user-turn system-reminders. Of the two contradicting halves
  only the opus-only prohibition lines (`Do not call the AgentTool…`)
  are patchable, which settles "strip one, decide which":
  `strip-agent-prohibitions` (dotfiles@70d67e3).

`strip-scratchpad-bloat` (dotfiles@88e946c) needed a prompt-patch feature:
replace text was inserted literally, so a condense couldn't keep the
session-generated path. `$NAME` in `replace.md` now re-emits the
search's same-named capture (mitmproxy@3cf4b8b).

The two opinion rewrites, `condense-delivering-work` (450→130 words,
dotfiles@7930800) and `condense-corrections` (250→70,
dotfiles@7710992), use verbatim-section `search.md` deliberately: any
upstream edit is a loud failed-to-match prompting a re-condense rather
than silently stripping stale text.

Final v2.1.221 deltas: opus −4035 (37.2%), fable −2006 (20.2%), sonnet
−5102 (18.3%). Dark matrix clean; warnings still only the two known
v2.1.128 ones.

## Tool-description patches (same day)

Extended `~/.claude/tool-description-patches.d/` from two patches to five.
Descriptions ride in `request.tools[]` and vary concurrently by prompt
family *and* by model (the Bash commit-trailer example bakes in the model
name), so `upstream.d/` accumulates one file per wording:

- `Monitor`: accepted the v2.1.221 wording (adds a PushNotification
  tie-in already covered by the must-read entry); incident archived.
- `EndConversation` (4.5k → 0.5k): policy moved to a new must-read entry.
- `Bash` (10k/1.3k → 1.1k): the classic ~10k long form still serves
  fable/sonnet sessions; its commit/PR walkthrough is dropped outright as
  user-config-overridden, not relocated.
- `Agent` (6.7k/1.6k → 1.1k): prompt-writing guidance folded into the
  spawn must-read entry.

Also triaged nine `_uncaught-syspatch` AssertionErrors: transient
half-created patch dirs from this morning's live editing (patches load
per-request; every intermediate file state is briefly load-bearing).
Archived; prevention (stage outside, atomic `mv` in) documented in
`CLAUDE.kb/patch-failure-triage.md` and used for all of today's
tool-description work.

## The tripwire this incident was missing (same day)

`_strip-rate` (mitmproxy@6319f6d): the follow-on proposed under "Why
nothing was loud", now live. Key design turn: an offline check against
promoted fixtures could not have caught this incident -- the fixtures
were stale while live traffic regressed, and against those fixtures
everything measured fine. So the check runs in `prompt_patches.py` on every
patched main body: classify its shape (`prompt_shape.py`, extracted from
`survey_captures.py`), require stripped bytes ≥ floor(shape), where
floor = half of what the current patch set strips from the newest
promoted fixture of that shape, computed in-process. Per-shape (the
original design requirement) falls out naturally: each body is checked
against its own shape's floor, so no global average dilutes anything. A
body matching no known shape marker fires `unknown-shape` -- the
whole-rewrite case caught one step earlier. Floors today: long-form
2435B, harness 1540B, harness-fable 1003B, harness-opus 2017B.
