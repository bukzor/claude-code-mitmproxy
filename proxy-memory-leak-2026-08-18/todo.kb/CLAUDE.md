# Todo

Open actions for this investigation, Skill(llm-subtask) conventions,
frontmatter per `../todo.jsonschema.yaml`.

Belongs: work someone must still do -- evidence to fetch, decisions to
make, remediations to apply.
Does not belong: measures themselves (`../remediations.kb/` holds the
what/why/how; a todo here just points and says "apply it").

Mark `status: done` with a `closeout:` note rather than deleting.
