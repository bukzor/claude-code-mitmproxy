# yaml-language-server: $schema=https://json-schema.org/draft-07/schema
$schema: https://json-schema.org/draft/2020-12/schema
type: object
additionalProperties: false
required:
  - why
properties:
  why:
    description: Slugs of the entries (higher layer, or same layer) this entry realizes.
    type: array
    minItems: 1
    items:
      type: string
