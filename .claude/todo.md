---
managed-by: Skill(llm-subtask)
---

# Todo

Use subtasks, not sections for organization. Ordered by intended completion.
Narrative in `../session.kb/`.

- [ ] Restart the proxy at a quiet moment: syscapture's subagent-body
      routing (mitmproxy@5c48772) is committed but the running process
      predates it. Only syscapture waits on this -- syspatch/toolpatch
      edits are already live via mitmproxy hot-reload
