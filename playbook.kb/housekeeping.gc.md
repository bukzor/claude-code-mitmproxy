# housekeeping.gc

Nothing to address. A proxy start swept `log/patch-failures/`: transients
past their window expired unread, archives past theirs reclaimed. Not
emitted yet -- `gc_patch_failures` still announces to stderr only
(`.claude/todo.md`, the logging revamp); the shard appears when it is wired.
