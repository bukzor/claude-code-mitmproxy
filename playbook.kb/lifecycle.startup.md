# lifecycle.startup

Nothing to address: the record is the proxy announcing what it loaded, and
the watch never prints it. Not emitted yet -- the three `load`-hook
inventories still go to stderr only (`.claude/todo.md`, the logging revamp),
so the shard does not exist until one is wired. This entry exists so the
published name leads somewhere.
