# incident.strip-floor

A request's patches together removed less than every fixture of its shape
says they should: the `_strip-rate` floor tripped, which is a patch that has
quietly stopped matching, or rewrite drift no single patch reports. Triage
as a patch miss (`CLAUDE.kb/patch-failure-triage.md`, the `_strip-rate`
section), with one more suspect first: a fixture promoted with a
session-optional block inside its core inflates the floor until ordinary
traffic trips it, and the fix is then a `blocks.d/` rule, not a patch.
