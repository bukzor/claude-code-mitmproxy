# capture.subagent-prompt

Nothing to address. A subagent system prompt the proxy had not seen before
was captured (`prompt_capture.py`; the request shape it is read from,
`CLAUDE.kb/subagent-request-shape.md`). Same standing as
`capture.system-prompt`: the record answers when a body first arrived, and
coverage is the promotion pass's question.
